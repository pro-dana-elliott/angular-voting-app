import { MinValidateDirective } from './min-validate.directive';

describe('MinValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new MinValidateDirective();
    expect(directive).toBeTruthy();
  });
});
